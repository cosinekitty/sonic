
program repeat ( x: in wave, y: out wave, count: integer )
{
    var k=0: integer;   // all numbers initialized to 0 by default

    while ( k < count )
    {
        y[c,i] << x[c,i];
        k += 1;
    }
}
